using UnityEngine;
using Core;
using Database;

public class MakeSound : MonoBehaviour
{
    /*private void OnTriggerEnter(Collider other)
    {
        GameManager gameManager = GameManager.Instance;
        if (gameManager == null)
        {
            return;
        }

        if (gameManager.IsSoundsOn)
        {
            /*AudioClip a = ResourcesDatabase.ReadSound("Table");
            AudioSource.PlayClipAtPoint(a, transform.position, 0.8f);#1#
        }
    }*/
}
